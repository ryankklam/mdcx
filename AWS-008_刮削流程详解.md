# AWS-008 番号刮削流程详解

## 测试环境信息
- Python 版本: 3.13.13
- 项目版本: MDCx 2.0.0
- 测试日期: 2026-05-24
- 测试番号: AWS-008

---

## 刮削流程总览

AWS-008 番号的刮削流程分为 **8 个主要步骤**，涉及番号识别、类型判断、网站选择、配置加载、爬虫初始化、任务准备和实际刮取。

---

## 详细步骤分析

### 【Step 1】番号识别

**输入数据：**
- 文件名：`AWS-008`
- 处理函数：[get_file_number()](file:///workspace/mdcx/number.py#L106-L248)

**识别结果：**
- ✅ 成功提取番号：**`AWS-008`**
- 识别方法：使用正则表达式匹配 `[A-Z]{2,}-\d{2,}` 格式

**代码逻辑：**
```python
# 从文件名中提取番号
file_number = get_file_number("AWS-008", [])
# 结果: AWS-008
```

---

### 【Step 2】番号类型判断

**判断逻辑：**

1. **是否无码流出** ([is_uncensored()](file:///workspace/mdcx/number.py#L9-L57))
   - 检查是否符合无码特征（如 `n\d{4}` 或 `XXX.XX.XX` 格式）
   - ✅ 结果：**不是无码流出**

2. **是否素人番号** ([is_suren()](file:///workspace/mdcx/number.py#L60-L63))
   - 检查是否符合素人番号特征（如 `SIRO`、`KIRAY` 等前缀）
   - 检查是否在 [SUREN_DIC](file:///workspace/mdcx/manual.py#L382-L415) 中
   - ✅ 结果：**不是标准素人番号**

**AWS-008 特殊性：**
- AWS 系列是 Amazon 原创内容，属于特殊系列
- 虽然不在标准素人列表中，但会使用素人网站组进行刮削
- 这是因为 AWS 番号格式与素人番号类似（字母-数字格式）

---

### 【Step 3】选择刮削网站组

**判断逻辑：**
根据番号格式和系统规则，选择使用 **website_suren**（素人网站组）进行刮削。

**素人网站组包含的网站：**
| 序号 | 网站代码 | 网站名称 |
|------|---------|---------|
| 1 | 7mmtv | 7mmtv |
| 2 | avsex | AVsex |
| 3 | freejavbt | FreeJavBT |
| 4 | jav321 | Jav321 |
| 5 | javbus | JavBus |
| 6 | javdb | JavDB |
| 7 | mgstage | MGStage |

**选择原因：**
- AWS 番号格式与素人番号相似
- AWS 系列通常在素人网站上收录
- 素人网站组专门处理此类番号

---

### 【Step 4】加载配置

**配置来源：**
- 配置文件：[mdcx/config/models.py](file:///workspace/mdcx/config/models.py)
- 配置类：[Config](file:///workspace/mdcx/config/models.py#L95-L525)

**基础配置：**
- 媒体路径：`./media`
- 有码网站组：19 个网站
- 素人网站组：7 个网站
- 无码网站组：9 个网站
- 国产网站组：5 个网站

**字段配置优先级：**

| 字段 | 网站优先级 | 语言 |
|------|-----------|------|
| **title** | theporndb → official → dmm → javdb | 日语 (jp) |
| **outline** | theporndb → official → dmm → javdb | 日语 (jp) |
| **poster** | theporndb → dmm | 未定义 |
| **actors** | theporndb → official → dmm → javdb | 日语 (jp) |

---

### 【Step 5】字段优先级配置

**字段配置详情：**

#### TITLE（标题）
```python
site_prority: [Website.THEPORNDB, Website.OFFICIAL, Website.DMM, Website.JAVDB]
language: Language.JP
```
- 优先从 ThePornDB 获取标题
- 如果失败，尝试 Official → DMM → JavDB
- 语言偏好：日语

#### OUTLINE（简介）
```python
site_prority: [Website.THEPORNDB, Website.OFFICIAL, Website.DMM, Website.JAVDB]
language: Language.JP
```
- 优先从 ThePornDB 获取简介
- 支持多语言翻译

#### POSTER（海报）
```python
site_prority: [Website.THEPORNDB, Website.DMM]
language: Language.UNDEFINED
```
- 优先从 ThePornDB 获取海报
- 海报通常不需要语言区分

#### ACTORS（演员）
```python
site_prority: [Website.THEPORNDB, Website.OFFICIAL, Website.DMM, Website.JAVDB]
language: Language.JP
```
- 优先从 ThePornDB 获取演员列表

---

### 【Step 6】准备刮削任务

**任务参数：**
```python
CrawlerInput:
  - number: "AWS-008"              # 番号
  - appoint_number: ""              # 指定番号（未使用）
  - appoint_url: ""                 # 指定网址（未使用）
  - mosaic: ""                      # 马赛克类型（未指定）
  - short_number: ""                # 短番号（未使用）
  - language: Language.UNDEFINED    # 语言（未定义）
  - org_language: Language.UNDEFINED  # 原始语言（未定义）
```

---

### 【Step 7】初始化爬虫提供者

**爬虫提供者：** [CrawlerProvider](file:///workspace/mdcx/crawler.py#L20-L48)

**初始化过程：**
1. 创建 [AsyncWebClient](file:///workspace/mdcx/web_async.py#L34-L150)
   - 超时时间：30 秒
   - 重试次数：3 次
2. 初始化 [BrowserProvider](file:///workspace/mdcx/browser.py)
   - 用于需要 JavaScript 渲染的网站
   - 默认不启用（可配置）
3. 创建爬虫实例缓存
   - 按需加载（懒加载）
   - 线程安全（使用 asyncio.Lock）

**架构图：**
```
┌─────────────────────────────────────┐
│     CrawlerProvider (爬虫提供者)     │
├─────────────────────────────────────┤
│  instances: Dict[Website, Crawler]  │
│  client: AsyncWebClient             │
│  browser_provider: BrowserProvider   │
│  lock: asyncio.Lock                 │
└─────────────────────────────────────┘
         │
         ├─→ AsyncWebClient (HTTP 客户端)
         │     └─→ httpx / curl-cffi
         │
         ├─→ BrowserProvider (浏览器自动化)
         │     └─→ patchright (Playwright)
         │
         └─→ 爬虫实例缓存
               ├─→ 7mmtv crawler
               ├─→ avsex crawler
               ├─→ freejavbt crawler
               ├─→ jav321 crawler
               ├─→ javbus crawler
               ├─→ javdb crawler
               └─→ mgstage crawler
```

---

### 【Step 8】执行刮削

**刮削流程：**

#### 8.1 创建文件扫描器
```python
file_scraper = FileScraper(config, crawler_provider)
```

#### 8.2 识别番号类型
- 分析番号格式
- 判断是否需要特殊处理
- AWS-008 识别为素人番号

#### 8.3 选择网站组
- 使用 **website_suren**（素人网站组）
- 包含 7 个网站

#### 8.4 按优先级尝试各网站

**执行顺序：**
1. **7mmtv**
   - 发送搜索请求：`https://7mmtv.sx/search/AWS-008`
   - 解析搜索结果
   - 如果找到，跳转到详情页
   - 如果失败，尝试下一个网站

2. **avsex**
   - 发送搜索请求
   - 解析搜索结果

3. **freejavbt**
   - 发送搜索请求
   - 解析搜索结果

4. **jav321**
   - 发送搜索请求
   - 解析搜索结果

5. **javbus**
   - 发送搜索请求
   - 解析搜索结果

6. **javdb**
   - 发送搜索请求
   - 解析搜索结果

7. **mgstage**
   - 发送搜索请求
   - 解析搜索结果

#### 8.5 字段数据提取

**对于每个成功的网站，按以下字段提取数据：**

| 字段 | 数据来源 | 处理方式 |
|------|---------|---------|
| title | 标题 | 语言转换（NFC） |
| originaltitle | 原标题 | 保持原语言 |
| outline | 简介 | 格式化、清理 HTML |
| actors | 演员列表 | 去重、格式化 |
| poster | 海报 URL | 下载并保存 |
| thumb | 缩略图 URL | 下载并保存 |
| release | 发行日期 | 格式化（YYYY-MM-DD） |
| runtime | 时长 | 转换为分钟 |
| score | 评分 | 格式化（如 8.5） |
| studio | 制作商 | 标准化 |
| tags | 标签 | 去重、分类 |

#### 8.6 数据聚合

**聚合策略：**
- 按字段优先级选择数据
- 如果高优先级网站失败，尝试低优先级
- 合并多个网站的数据

**示例：**
```python
# 标题从 javdb 获取
title = "AWS-008 - Amazon Original Series"
# 简介从 theporndb 获取
outline = "..."
# 海报从 dmm 获取
poster_url = "https://..."
```

#### 8.7 数据后处理

**后处理步骤：**
1. **字符转义**
   ```python
   "&amp;" → "&"
   "&lt;" → "<"
   "&gt;" → ">"
   ```

2. **日期格式化**
   ```python
   "2023/01/15" → "2023-01-15"
   ```

3. **评分格式化**
   ```python
   "8.500" → "8.5"
   ```

4. **标签清理**
   ```python
   ",HD高画质," → ","
   ```

5. **Unicode 标准化**
   ```python
   # macOS NFD → NFC
   unicodedata.normalize("NFC", text)
   ```

#### 8.8 返回结果

**返回数据类型：** [CrawlersResult](file:///workspace/mdcx/models/types.py#L58-L85)

**包含字段：**
```python
@dataclass
class CrawlersResult:
    number: str = ""                           # 番号
    title: str = ""                           # 标题
    originaltitle: str = ""                   # 原标题
    outline: str = ""                         # 简介
    actors: list[str] = field(default_factory=list)  # 演员列表
    all_actors: list[str] = field(default_factory=list)  # 所有演员
    poster: str = ""                          # 海报路径
    thumb: str = ""                           # 缩略图路径
    release: str = ""                         # 发行日期
    year: str = ""                            # 年份
    runtime: int = 0                          # 时长（分钟）
    score: str = ""                           # 评分
    studio: str = ""                          # 制作商
    publisher: str = ""                        # 发行商
    director: str = ""                         # 导演
    series: str = ""                          # 系列
    tags: list[str] = field(default_factory=list)      # 标签
    mosaic: str = ""                          # 马赛克类型
    ...
```

---

## 实际网络请求示例

### 搜索请求

**JavDB 示例：**
```
GET https://javdb.com/search?q=AWS-008&locale=zh
Headers:
  User-Agent: Mozilla/5.0 ...
  Accept: text/html,application/xhtml+xml,...
```

### 解析搜索结果

**HTML 解析：**
```python
# 使用 BeautifulSoup 解析
soup = BeautifulSoup(response.text, 'lxml')

# 查找视频卡片
video_cards = soup.select('.video-card')

for card in video_cards:
    # 提取番号
    title = card.select_one('.video-title')
    number = card.select_one('.uid')
    link = card.select_one('a')['href']
```

### 获取详情页

**详情页 URL：**
```
GET https://javdb.com/v/ABC123
```

**提取字段：**
```python
# 海报
poster = soup.select_one('.cover img')['src']

# 标题
title = soup.select_one('.title').text.strip()

# 简介
outline = soup.select_one('.synopsis').text.strip()

# 演员
actors = [a.text for a in soup.select('.actor a')]

# 发行日期
release = soup.select_one('.meta-row').text.strip()

# 评分
score = soup.select_one('.score').text.strip()
```

---

## 错误处理

### 网络错误
```python
try:
    response = await client.get(url)
except httpx.TimeoutException:
    print(f"请求超时: {url}")
    # 尝试下一个网站
except httpx.HTTPError as e:
    print(f"HTTP 错误: {e}")
    # 重试或跳过
```

### 解析错误
```python
try:
    title = soup.select_one('.title').text
except AttributeError:
    print(f"无法解析标题")
    # 使用备选方案
```

### 反爬虫机制
```python
# 使用 curl-cffi 模拟浏览器
# 添加随机延迟
await asyncio.sleep(random.uniform(1, 3))

# 使用代理（可选）
proxy = "http://proxy.example.com:8080"
```

---

## 性能优化

### 1. 懒加载爬虫
```python
# 只在需要时才创建爬虫实例
async def get(self, site: Website):
    if r := self.instances.get(site):
        return r
    async with self.lock:
        if site not in self.instances:
            # 创建新实例
            self.instances[site] = create_crawler(site)
    return self.instances[site]
```

### 2. 浏览器复用
```python
# 同一个浏览实例用于多个请求
if use_browser and self.browser is None:
    self.browser = await self.browser_provider.get_browser()
```

### 3. 并发限制
```python
# 使用 aiolimiter 限制并发
limiter = AsyncLimiter(10, 1.0)  # 每秒 10 个请求
```

---

## 测试验证

### 运行测试
```bash
cd /workspace
uv run python test_aws008_scrape.py
```

### 预期结果
- ✅ 番号识别成功
- ✅ 网站组选择正确
- ✅ 字段配置加载成功
- ✅ 爬虫提供者初始化成功
- ✅ 刮削流程展示完整

---

## 总结

AWS-008 的刮削流程展示了 MDCx 系统的核心功能：

1. **智能番号识别**：准确识别各种格式的番号
2. **灵活的网站选择**：根据番号类型选择合适的网站组
3. **字段级优先级**：支持不同字段从不同网站获取
4. **数据聚合**：智能合并多个来源的数据
5. **完善的错误处理**：处理网络错误、解析错误和反爬虫机制

---

## 相关文件

- [mdcx/number.py](file:///workspace/mdcx/number.py) - 番号识别
- [mdcx/config/models.py](file:///workspace/mdcx/config/models.py) - 配置管理
- [mdcx/crawler.py](file:///workspace/mdcx/crawler.py) - 爬虫提供者
- [mdcx/core/file_crawler.py](file:///workspace/mdcx/core/file_crawler.py) - 文件扫描器
- [mdcx/web_async.py](file:///workspace/mdcx/web_async.py) - HTTP 客户端
- [mdcx/manual.py](file:///workspace/mdcx/manual.py) - 手动配置
